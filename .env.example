# Example .env settings
# Option 1: single SMTP URL (like sendgrid)
# SMTP_URL=smtps://username:password@smtp.example.com:465

# Option 2: full SMTP configuration
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=username
# SMTP_PASS=password

# FROM_EMAIL (default used for nodemailer 'from' if not provided)
# FROM_EMAIL=space3darte@gmail.com
# TO_EMAIL=space3darte@gmail.com

# PORT for the server
# PORT=3000
